# TODO
A simple React-based To-Do List App that allows users to add and delete tasks. Each task is stored with a unique ID using uuid. This project demonstrates the use of React hooks like useState, dynamic rendering using .map(), and array operations like .filter() to update the task list.
